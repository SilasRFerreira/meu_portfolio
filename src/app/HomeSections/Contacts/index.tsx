import ContactsList from "./ContactsList"
import Greetings from "./Greetings"

const Contacts = () => {
    return(
        <section id="contacts">
            <Greetings/>
            <ContactsList/>
        </section>
    )
}

export default Contacts